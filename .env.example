# ====================================================================
# 🔥 Global Configuration
# ====================================================================

# Base URL for the cloud backend API where the ESP32 will send alerts
# e.g., https://your-firebase-function-url.cloudfunctions.net/api
REACT_APP_API_BASE_URL="[Your Backend API Endpoint URL]"


# ====================================================================
# ☁️ Cloud Backend (Node.js/Python/Firebase Functions)
# ====================================================================

# --- Firebase Admin SDK Credentials ---
# Typically loaded from a service account JSON file (e.g., serviceAccountKey.json)
# If using Firebase Functions, these are often set automatically or via environment variables in the console.
FIREBASE_PROJECT_ID="your-firebase-project-id"
FIREBASE_PRIVATE_KEY="[The private key from your service account file, escaped if needed]"
FIREBASE_CLIENT_EMAIL="your-service-account-email@appspot.gserviceaccount.com"
FIREBASE_DATABASE_URL="https://your-firebase-project-id-default-rtdb.firebaseio.com"

# --- Twilio API Credentials for SMS/Call Notifications ---
# Used for Proximity-Aware Notification feature
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_SENDER_NUMBER="+15005550006" # The Twilio number that will send the alerts

# --- Geolocation Configuration ---
# Coordinates for the central tourist zone, used for initial geofencing logic
DEFAULT_ZONE_LATITUDE="34.052235"
DEFAULT_ZONE_LONGITUDE="-118.243683"
DEFAULT_ZONE_RADIUS_METERS="500" # Radius to check for proximity-aware authority notifications

# --- AI Verification Service (If external to main backend) ---
# If you are using an external service or separate ML model deployment
# AI_MODEL_ENDPOINT="https://your-ml-verification-api.com/verify"
# AI_MODEL_API_KEY="your-ml-api-key"

# ====================================================================
# 📍 ESP32 Hardware (Firmware Configuration)
# ====================================================================

# --- Wi-Fi Credentials for connecting the ESP32 to the internet ---
# NOTE: These are often stored directly in the firmware or a separate credentials header file (.h)
# For local testing, using environment variables is simpler.
WIFI_SSID="Your_Wifi_Network_Name"
WIFI_PASSWORD="Your_Wifi_Password"

# --- Device Security Key ---
# A secret key or token the device uses to authenticate with the backend API
DEVICE_AUTH_SECRET="[A strong secret key for device-to-cloud authentication]"

# --- Device Identifier ---
# The unique ID for the specific panic button hardware unit
DEVICE_ID="ESP32_PANIC_001"


# ====================================================================
# 🌐 Web Frontend (React.js Dashboard)
# ====================================================================

# --- Google Maps API Key ---
# Required for the Live Dashboard's visualization and geofencing features
# Should be restricted to only run on your web domain for security
REACT_APP_GOOGLE_MAPS_API_KEY="AIzaSyXXXXXXXXXXXXXXXXXX"

# --- Firebase Client SDK Configuration (for real-time updates) ---
# The public-facing keys for the web app to connect to Firestore/Realtime Database
REACT_APP_FIREBASE_API_KEY="AIzaSyXXXXXXXXXXXXXXXXXX"
REACT_APP_FIREBASE_AUTH_DOMAIN="your-firebase-project-id.firebaseapp.com"
REACT_APP_FIREBASE_PROJECT_ID="your-firebase-project-id"
REACT_APP_FIREBASE_STORAGE_BUCKET="your-firebase-project-id.appspot.com"
REACT_APP_FIREBASE_MESSAGING_SENDER_ID="123456789012"
REACT_APP_FIREBASE_APP_ID="1:123456789012:web:abcdef123456"

# --- Dashboard Security ---
# Secret for securing the dashboard login/session (if not using Firebase Auth)
# REACT_APP_DASHBOARD_SECRET="another-strong-secret"